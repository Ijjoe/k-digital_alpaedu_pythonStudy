from setuptools import setup, find_packages

setup(
    name='alpaco9cijlib',
    version='0.1',
    packages=find_packages(),
    install_requires=[
    'google_colab_selenium'
    ],
    # 추가 메타데이터
)
