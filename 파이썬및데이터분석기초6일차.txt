
### seaborn 라이브러리

import seaborn as sns
import matplotlib.pyplot as plt 

fig = plt.figure()

ax1 = fig.add_subplot(1,1,1)

sns.kdeplot(데이터) 밀도함수
sns.countplot(데이터)  특정값 갯수 파악
sns.lmplot  산점도
sns.jointplot  산점도 + 히스토그램
sns.barplot 평균 막대 그래프
sns.boxplot 상자 그래프
sns.violinplot 바이올린 그래프
sns.pairplot 관계 그래프
sns.histplot 히스토그램

단변량과 다변량 개념 이해하기
hue 그래프상의 개별 색상 가능으로 한차원 올린수있음

sns.set_style(배경) 


tips = sns.load_dataset("tips")

###pandas 라이브러리

import pandas as pd


tips['total_bill'].plot.hist() 히스토그램
tips.plot.scatter() 산점도
tips.plot.hexbin) 육각형 산점도
df.plot.pie() 원형 그림


### 여러개의 데이터 프레임 합치기
import pandas as pd

concat() # 열방향 기준으로 합침
concat(~,axis=1) # 행방향 기준으로 합침 

concat(~,ignore_index=~) 는 행렬번호 인덱스 순서대로 넣을수있는옵션



### 데이터프레임 merge

df1.merge(df2, letf_on=df1의기준열, right_on=df2의기준열)



### 누락값 처리 방법

데이터 분석시 누락값있으면 원활하게 연산이 안됨
누락값을 처리 하는 방식

from numpy import NAN NaN nan

누락값은 비교할수 없음
단 pd.isnull() 함수로 누락값 판단가능

df.count() 널값은 갯수에 미포함함
fillna(값) 누락값 채우기
df.interpolate() 누락값을 흐름 분석해서 채워넣어줌


### 개별참조 조사 내용
import pandas as pd
technologies = {
    'Courses':["Spark","PySpark","Python","Pandas","Python"],
    'Fee' :[20000,25000,22000,30000,25000],
    'Duration':['30days','40days','35days','50days','40days'],
    'Discount':[1000,2300,1200,2000,2300]
              }
df = pd.DataFrame(technologies)
print(df)

# Converting first column to Series
ser = df.iloc[:,0]
print(ser)

# Display type
print(type(ser))

# Use squeeze() method
ser = df['Courses'].squeeze()
print(ser)

# Display type
print(type(ser))

# Converting specific column to Series
ser = df.iloc[:,2]
print(ser)

# Display type
print(type(ser))

# Converting duration column to Series
ser = df['Duration'].squeeze()
print(ser)

# Display type
print(type(ser)
      
# Convert multiple columns of DataFrame to Series
ser1 = df.iloc[:,1]
print(ser1)

ser2 = df.iloc[:,2]
print(ser2)

ser3 = df.iloc[:,3]
print(ser3)

# Convert DataFrame row to Series
ser = df.iloc[2].reset_index(drop=True).squeeze()
print(ser)

# Display type
print (type(ser))












